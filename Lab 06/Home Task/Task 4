#include<stdio.h>
int main(){
	int num,i,j;
	printf("Enter value of n: ");
	scanf("%d",&num);
	for (i=1;i<=num;i++){
		printf("\nMultiplication table of %d:\n",i);
		for(j= 1;j<=10;j++){
			printf("%d * %d = %d\n",i,j,i*j);
		}
	}
	return 0;
}
